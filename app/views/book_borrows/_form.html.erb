<%= form_with(model: book_borrow) do |form| %>
<% if book_borrow.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(book_borrow.errors.count, "error") %> prohibited this book_borrow from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% book_borrow.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :coupon_borrow_id, class: label_class %>
          <%= form.number_field :coupon_borrow_id, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :title, class: label_class %>
          <%= form.text_field :title, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :author_id, class: label_class %>
          <%= form.number_field :author_id, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :publisher_id, class: label_class %>
          <%= form.number_field :publisher_id, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if book_borrow.persisted? %>
<%= link_to "Cancel", book_borrow, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", book_borrows_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
